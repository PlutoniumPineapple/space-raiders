if !bounced {
	direction = direction + random(360)
	alarm[0] = 20;
	bounced = true;
	
	new_size += growth 

	image_xscale = Approach(image_xscale, new_size, 1)
	image_yscale = Approach(image_yscale, new_size, 1)
}
